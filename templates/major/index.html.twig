{% extends 'base.html.twig' %}

{% block title %}Major index{% endblock %}

{% block body %}
    <h1>Major index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>MajorName</th>
                <th>MajorInfo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for major in majors %}
            <tr>
                <td>{{ major.id }}</td>
                <td>{{ major.MajorName }}</td>
                <td>{{ major.MajorInfo }}</td>
                <td>
                    <a href="{{ path('app_major_show', {'id': major.id}) }}">show</a>
                    <a href="{{ path('app_major_edit', {'id': major.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_major_new') }}">Create new</a>
{% endblock %}
